<?xml version="1.0" encoding="utf-8"?>
<!--
  components/relink-title-form.tsx: "Wrong match? Fix ID". Fill in whichever
  id you have; the server checks tmdbId, then imdbId, then tvdbId (TV only).
-->
<ContentDialog
    x:Class="Marquee.Windows.Controls.RelinkDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Fix this title's match"
    PrimaryButtonText="Save"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    IsPrimaryButtonEnabled="False">

    <StackPanel Spacing="12" MinWidth="380">
        <TextBlock
            Text="Fill in whichever id you have. This repoints your synced library to the correct title without needing to fix the match in Plex, Jellyfin or Sonarr itself."
            Style="{StaticResource BodyTextBlockStyle}"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            TextWrapping="Wrap" />
        <TextBox x:Name="TmdbBox" Header="TMDb ID" IsSpellCheckEnabled="False" TextChanged="OnTextChanged" />
        <TextBox x:Name="ImdbBox" Header="IMDb ID (tt…)" IsSpellCheckEnabled="False" TextChanged="OnTextChanged" />
        <TextBox x:Name="TvdbBox" Header="TVDB ID" IsSpellCheckEnabled="False" Visibility="Collapsed" TextChanged="OnTextChanged" />
    </StackPanel>
</ContentDialog>
